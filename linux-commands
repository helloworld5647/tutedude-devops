ls - to list all the directories and files 
      eg:-  ls /home
      claude  ubuntu

pwd - used to print the all current working directory
      eg:- pwd
      /home/user

cd -  used to change the directory
      eg:-cd /etc         
       cd ..        
       cd ~            
       cd -

mkdir -  used to create a  directories 
      eg:-mkdir demo_dir
       ls demo_dir
      hello.txt  renamed.txt  secure.txt  subdir/

mv -  used to move or rename the directories

      eg:-mv demo_dir/newfile.txt demo_dir/renamed.txt   
       mv demo_dir/renamed.txt /tmp/                   
       ls demo_dir
      hello.txt  secure.txt  subdir/

cp - same uses as mv command or used to copy the files

      eg:-cp demo_dir/hello.txt demo_dir/copy.txt
       ls demo_dir
      copy.txt  hello.txt  renamed.txt  secure.txt  subdir/


rm - used to remove or delete the files or directorieseg:-

      eg:-rm demo_dir/copy.txt             
      rm -r demo_dir/subdir            
      ls demo_dir
      hello.txt  renamed.txt  secure.txt  subdir/


touch - used to  create  a empty /new fileeg:-

      eg:-touch demo_dir/newfile.txt
      ls demo_dir
      hello.txt  newfile.txt  renamed.txt  secure.txt  subdir/

ln - used to create a symbolic links or shortcuts to other fileseg:-

      eg:-ln -s demo_dir/hello.txt mylink.txt
      ls -l mylink.txt
      lrwxrwxrwx 1 root root 17  mylink.txt -> demo_dir/hello.txt

cat - used to show diplay the file content to the terminaleg:-

      eg:-cat demo_dir/hello.txt
      Hello from cat

clear - used the clear the terminal window

echo - used to print the entered text 

      eg:-echo "Hello, Linux!"
      Hello, Linux!

man - used to access the manual of all linux command

      eg:-man ls

uname - used to get the os info

      eg:-uname -a
      Linux runsc 4.4.0

whoami - used to get the current active session username

    eg:-whoami
    root

tar - used to extract and compress the files

      eg:-ar -czf demo.tar.gz demo_dir/          
      -rw-r--r-- 1 root root 230  demo.tar.gz
      
       tar -xzf demo.tar.gz -C /tmp/           
       ls /tmp/demo_dir
      copy.txt  hello.txt  renamed.txt  secure.txt  subdir/

grep - used to search for a string in an output

      eg:-grep "root" /etc/passwd
      root:x:0:0:root:/root:/bin/bash

head - return the specified number of lines from the output from top

      eg:-head -3 /etc/passwd
      root:x:0:0:root:/root:/bin/bash
      daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
      bin:x:2:2:bin:/bin:/usr/sbin/nologin

tail - return the specified number of lines from the output form bottom 

      eg:-tail -3 /etc/passwd
      systemd-network:x:998:998:systemd Network Management:/:/usr/sbin/nologin
      messagebus:x:100:101::/nonexistent:/usr/sbin/nologin
      polkitd:x:997:997:User for polkitd:/:/usr/sbin/nologin


export - used to export the environment variables in linux

      eg:-export MY_VAR="Hello Linux"
       echo $MY_VAR
      Hello Linux

zip - used to create a zip files in linux

      eg:-zip -r demo.zip demo_dir/
      -rw-r--r-- 1 root root 1018  demo.zip

unzip -  used to unzip the files in linux

      eg:-unzip demo.zip -d /tmp/unzipped/
       ls /tmp/unzipped
      demo_dir/

ssh - it is  a secure shell command in linux

      eg:-ssh user@192.168.1.10           
       ssh user@example.com            
       ssh -p 2222 user@example.com

service - used to start or stop the services in linux

      eg:-service nginx start
       service nginx stop
       service nginx status


ps -  used to display the active processes

      eg:-ps aux | head -4
      USER    PID  %CPU  %MEM  COMMAND
      root      1   1.3   0.1  /process_api
      root     52   7.4   0.0  /bin/sh
      root     88  50.0   0.0  ps aux

kill & killall - used to kill thr inouted task or all the current tasks or used to kill the processes by their ID or Nme

      eg:-kill 1234               
       kill -9 1234         
       killall firefox

df - used to prnt the information about the disk filesystem

      eg:-df -h /
      Filesystem   Size  Used  Avail  Use%  Mounted on
      none         9.9G  3.4M  9.9G    1%  /

chmod - this used for changing the permission of the files

      eg:- chmod 444 demo_dir/secure.txt    
       ls -l demo_dir/secure.txt
      -r--r--r-- 1 root root 0  demo_dir/secure.txt
      
       chmod 755 script.sh                
       chmod +x script.sh  

ifconfig - used to list or display the network information like interfaces and ip address

      eg:-ifconfig                          
       ip addr show                      
      eth0: inet 192.168.1.5/24
      lo:   inet 127.0.0.1/8


wget -  used iin linux for direct download from the provided link

      eg:-wget https://example.com/file.txt
      
      ufw - this is the linux firewall command
      
      eg:-ufw enable                       
       ufw allow 22                       
       ufw deny 80                    
       ufw status 

apt, pacman, yum, rpm - this are the package managers commands used to isntall delete or update the packages

      eg:-apt install curl                   
       apt remove curl                    
       apt update                    
       yum install curl              
       pacman -S curl                 
       rpm -ivh package.rpm             


sudo - used to escalate the priviledges in linux

      eg:-sudo apt update                    
       sudo rm /etc/hosts               
       sudo -i 

alias - used to create custom shortcuts for your regularly used commands

      eg:- alias cls='clear'
       alias | grep cls
      cls='clear'
      
       alias ll='ls -la'             
       alias ..='cd ..'  

whereis - used to locate the binary , source ,  and manual pges of the command

      eg:-whereis ls
      ls: /usr/bin/ls


whatis - used to find for what the command is used for 

      eg:-whatis ls
      ls (1) - list directory contents
      
      whatis grep
      grep (1) - print lines that match patterns

top - used to view the active processes live with their system usage

      eg:-top
      top - 21:02:15 up 3 min | load avg: 0.00, 0.00, 0.00
      Tasks: 4 total, 1 running, 3 sleeping
      %Cpu: 0.0 us | MiB Mem: 9216 total, 9199 free
      
        PID  USER   %CPU  %MEM  COMMAND
    1  root   30.0   0.1  process_api
   52  root    0.0   0.0  sh
   90  root    0.0   0.1  top






Assignment :- 
1. Creating and Renaming Files/Directories
Create a directory named test_dir using mkdir.
Inside test_dir, create an empty file called example.txt.
Rename example.txt to renamed_example.txt using mv


Ans:- 
– mkdir test_dir
– touch test_dir/harsh1.txt
– mv test_dir/harsh1.txt test_dir/harsh.txt
– ls
–  cd test_dir
–  ls

2. Viewing File Contents
Use cat to display the contents of /etc/passwd.
Display only the first 5 lines of /etc/passwd using head.
Display only the last 5 lines of /etc/passwd using tail.
Ans:- 
– cat /etc/passwd

– head -5 /etc/passwd
– tail -5 /etc/passwd


3.Searching for Patterns
Use grep to find all lines containing the word "root" in /etc/passwd.
Ans:- 
 – grep root /etc/passwd

4. Zipping and Unzipping
Compress the test_dir directory into a file named test_dir.zip using zip.
Unzip test_dir.zip into a new directory named unzipped_dir.
Ans:- 
– zip -r test_dir.zip . -i test_dir/
– mkdir unzipped_dir
– unzip test_dir.zip -d unzipped_dir/


5. Downloading Files
Use wget to download a file from a URL (e.g., https://example.com/sample.txt).

Ans:- 
– wget https://docs.google.com/document/d/1O5rvyUFY7G1_lK6wrogsnDIBGuySIbXl5W3bP3D3OrA/edit?tab=t.0



6. Changing Permissions
Create a file named secure.txt and change its permissions to read-only for everyone using chmod.

Ans:-
–  touch secure.txt
– chmod 444 secure.txt
 

7. Working with Environment Variables
Use export to set a new environment variable called MY_VAR with the value "Hello, Linux!".

Ans:- 
– export MY_VAR="Hello, Linux!"
– echo $MY_VAR


